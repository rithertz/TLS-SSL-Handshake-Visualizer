// Vitest setup file — imports the official @testing-library/jest-dom matchers
// (e.g. toBeInTheDocument, toHaveTextContent) so they are available globally
// in every test file without manual per-file imports.
import "@testing-library/jest-dom";
